#include <iostream>
#include <cmath>
using namespace std;
int a[100001] = {0};
int main() {
    int n, t, maxn = -1, minn = 9999999;
    int no, more;
    cin >> n;
    while (cin >> t) {
        a[t]++;
        maxn = max(maxn, t);
        minn = min(minn, t);
    }
    for (int i = minn; i <= maxn; i++) {
        if (a[i] == 0) no = i;
        if (a[i] == 2) more = i;
    }
    cout << no << " " << more;
    return 0;
}
