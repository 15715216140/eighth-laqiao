#include <iostream>
#define PI 3.1415926536
using namespace std;
int main() {
    double r;
    cin >> r;
    double v = 4.0 / 3 * PI * r * r * r;
    double sv = 4.0 * PI * r * r;
    double s = PI * r * r;
    printf("%-20.10f %-20.10f %-20.10f", s, sv, v);
    return 0;
}