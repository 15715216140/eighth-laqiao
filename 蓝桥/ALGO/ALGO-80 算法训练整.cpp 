#include <iostream>
char c[1000][1000];
using namespace std;
int main() {
    int n, sum = 0, a[1000];
    cin >> n;
    for(int i = 0; i < n; i++)
        cin >> a[i];
    for(int i = 0; i < n; i++)
        sum += *(a+i);
    cout << sum / n;
    return 0;
}
