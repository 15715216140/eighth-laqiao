#include <iostream>
using namespace std;
int re(int n){
    int ans = 0;
    while(n){
        ans = ans * 10 + n % 10;
        n /= 10;
    }
    return ans;
}
int main() {
    int a, b;
    cin >> a >> b;
    cout << re(re(a)+re(b));
    return 0;
}